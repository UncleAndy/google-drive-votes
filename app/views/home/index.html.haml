%h2{ :align => 'center' } Сеть доверия на основе документов Google

%p{ :align => 'center' }
  = link_to '#' do
    %button.btn.btn-default.btn-lg= I18n.t("titles.thrust_net")
%p{ :align => 'center' }
  = link_to user_path do
    %button.btn.btn-primary.btn-lg= I18n.t("titles.user_cabinet")

/
  %p User session is #{(user_session ? "OK" : "BAD")}
